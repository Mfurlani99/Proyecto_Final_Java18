---
title: "Database: Alinear usuarios, User y roles"
status: "open"
priority: "high"
projects: ["[[MiTienda Java Web]]"]
contexts: ["database"]
phase: "Database"
executionOrder: 2
timeEstimate: 60
---

## Objetivo
Alinear la tabla `usuarios`, la clase `User` y el enum `Rol`.

## Criterio de completado
- [ ] Los campos de `usuarios` coinciden con `User`.
- [ ] Los valores de `rol` coinciden entre SQL y Java.
